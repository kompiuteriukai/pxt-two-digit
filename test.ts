basic.forever(() => {
	
})
input.onButtonPressed(Button.A, () => {
    custom.twoDigit(-5)
})
input.onButtonPressed(Button.AB, () => {
    custom.twoDigit(0)
})
input.onButtonPressed(Button.B, () => {
    custom.twoDigit(37)
})
